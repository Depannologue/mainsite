%div.some{ :style => "position: absolute; left: 300px; top: 300px;" }
  %ul
    - @intervention_parents.each do |intervention|
      %li
        = link_to intervention.short_description, intervention_type_parent_show_path(intervention_parent: intervention.short_description)
